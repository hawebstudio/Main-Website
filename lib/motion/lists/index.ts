import { staggerContainer, staggerContainerFast, staggerItem } from '../stagger'

export const gridReveal = staggerContainer
export const timelineReveal = staggerContainerFast
export const collectionItemReveal = staggerItem
