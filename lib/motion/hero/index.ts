import { fadeUp } from '../fade'
import { staggerContainer } from '../stagger'

export const heroContent = staggerContainer
export const heroTitle = fadeUp
export const heroImage = fadeUp
export const heroCta = fadeUp
