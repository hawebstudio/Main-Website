import { cardReveal } from '../cards'
import { fadeUp } from '../fade'
import { heroContent } from '../hero'
import { gridReveal } from '../lists'
import { pageEnter, sectionReveal } from '../page'

export const heroReveal = heroContent
export { cardReveal, gridReveal, pageEnter, sectionReveal }
export const modal = fadeUp
export const drawer = fadeUp
