# Utility Scripts

General-purpose script helper area.

V1 keeps reusable implementation in `scripts/shared/toolkit.ts`. Add focused utilities here only when a second reusable helper file is justified.

Scripts: none in Version 1.

Inputs: none.

Outputs: none.

Usage: keep this folder reserved for reusable helper modules that are not shared pipeline primitives.

Dependencies: none.

Future expansion: CLI parsing helpers, colorized console output, and file format helpers can move here once there is repeated use.
